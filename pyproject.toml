[project]
name = "trusted-search"
version = "0.1.0"
description = "Grounded hybrid search with BM25, vector retrieval, and citations."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
